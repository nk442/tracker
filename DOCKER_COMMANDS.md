# Docker - Краткая инструкция

## Основные команды для работы с трекером

### 🚀 Запуск проекта

```bash
# Запустить контейнеры (первый раз)
docker-compose up -d

# Запустить с пересборкой (если изменили код)
docker-compose up -d --build
```

### 📋 Просмотр статуса и логов

```bash
# Посмотреть статус контейнеров
docker-compose ps

# Посмотреть логи приложения
docker-compose logs app

# Посмотреть логи в реальном времени (для отладки)
docker-compose logs -f app

# Последние 100 строк логов
docker-compose logs --tail=100 app

# Логи базы данных
docker-compose logs db
```

### 🔄 Управление контейнерами

```bash
# Перезапустить приложение (быстрая перезагрузка)
docker-compose restart app

# Остановить контейнеры (данные сохраняются)
docker-compose stop

# Запустить остановленные контейнеры
docker-compose start

# Остановить и удалить контейнеры (данные в БД сохраняются)
docker-compose down

# Остановить и удалить всё, включая данные БД
docker-compose down -v
```

### 🔨 Пересборка при изменениях

```bash
# Пересобрать только приложение (без кэша)
docker-compose build --no-cache app

# Пересобрать и сразу запустить
docker-compose up -d --build app
```

### 💾 Работа с тестовыми данными

```bash
# Добавить тестовые данные в БД
docker-compose exec app python add_test_data.py

# Войти в контейнер приложения (для отладки)
docker-compose exec app bash

# Подключиться к PostgreSQL
docker-compose exec db psql -U tracker_user -d tracker_db
```

### 🧹 Очистка и решение проблем

```bash
# Удалить все контейнеры и пересоздать
docker-compose down
docker-compose up -d --build

# Полная очистка (если что-то сломалось)
docker-compose down -v
docker system prune -f
docker-compose build --no-cache
docker-compose up -d

# Удалить образ приложения вручную
docker images | grep tracker
docker rmi tracker_app
```

### 🔍 Диагностика

```bash
# Список всех контейнеров
docker ps -a

# Список образов
docker images

# Использование диска Docker
docker system df

# Информация о контейнере
docker inspect tracker_app_1

# Проверить сеть
docker network ls
docker network inspect tracker_default
```

### 🌐 Доступ к приложению

После запуска:
- **Веб-интерфейс:** http://localhost:8000
- **API:** http://localhost:8000/api/event
- **PostgreSQL:** localhost:5432

### ⚙️ Полезные комбинации

```bash
# Перезапустить с чистыми логами
docker-compose restart app && docker-compose logs -f app

# Быстрое обновление после изменения кода
docker-compose restart app

# Полная пересборка при больших изменениях
docker-compose down && docker-compose build --no-cache && docker-compose up -d

# Очистить всё и начать заново
docker-compose down -v && docker-compose up -d --build
```

### 🐛 Если что-то не работает

1. **Проверьте логи:**
   ```bash
   docker-compose logs -f app
   ```

2. **Перезапустите контейнеры:**
   ```bash
   docker-compose restart app
   ```

3. **Пересоберите без кэша:**
   ```bash
   docker-compose build --no-cache app
   docker-compose up -d
   ```

4. **Полная переустановка:**
   ```bash
   docker-compose down -v
   docker system prune -f
   docker-compose up -d --build
   ```

### 📝 Примечания

- Флаг `-d` = detached mode (запуск в фоне)
- Флаг `-f` = follow (следить за логами в реальном времени)
- Флаг `-v` = volumes (удалить тома с данными)
- Используйте `sudo` если получаете ошибки прав доступа
- Данные БД хранятся в Docker volume `postgres_data`
- При `docker-compose down` данные БД сохраняются
- При `docker-compose down -v` данные БД удаляются

### 🎯 Типичный рабочий процесс

```bash
# Утром - запустить проект
docker-compose up -d

# В течение дня - смотреть что происходит
docker-compose logs -f app

# После изменения кода - быстрый перезапуск
docker-compose restart app

# Вечером - остановить
docker-compose stop

# Или оставить работать в фоне
# (контейнеры будут запускаться автоматически при перезагрузке системы)
```



# 1. Остановить и удалить контейнеры + volumes (удалит данные БД)
sudo docker-compose down -v

# 2. Удалить образы проекта
sudo docker images | grep tracker
sudo docker rmi tracker-app tracker_app

# 3. Удалить образ PostgreSQL (если не используется в других проектах)
sudo docker images | grep postgres
sudo docker rmi postgres:15-alpine

# 4. Очистить неиспользуемые ресурсы Docker
sudo docker system prune -f

# 5. (Опционально) Очистить всё, включая кэш сборки
sudo docker builder prune -a -f