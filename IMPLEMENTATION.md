# Click Tracker - Реализация

## ✅ Что реализовано

### Backend (FastAPI)

#### 1. Структура проекта
```
tracker/
├── app/
│   ├── main.py              # FastAPI приложение с lifespan
│   ├── config.py            # Настройки через Pydantic Settings
│   ├── database.py          # Асинхронный пул подключений (asyncpg)
│   ├── models/
│   │   └── schemas.py       # Pydantic модели для валидации
│   ├── routers/
│   │   ├── api.py           # API endpoints для приема событий
│   │   └── pages.py         # HTML страницы с HTMX
│   └── templates/           # Jinja2 шаблоны
├── static/                  # CSS стили
├── init.sql                 # SQL схема базы данных
└── requirements.txt         # Python зависимости
```

#### 2. База данных (PostgreSQL)

**Таблица campaigns:**
- Хранит информацию о кампаниях (название, offer URL)
- Auto-increment ID для уникальной идентификации

**Таблица events:**
- Хранит все события (email_click, landing_click, conversion)
- Связь с campaigns через foreign key
- JSONB поле для дополнительных параметров
- Индексы для быстрого поиска по campaign_id, email, domain

#### 3. API Endpoints

**GET /api/event** - Прием событий
- Параметры: cid, event, email, domain
- Любые дополнительные параметры сохраняются в JSONB
- Валидация типа события (email_click, landing_click, conversion)
- Проверка существования кампании
- Автоматическое извлечение IP и User-Agent
- Возвращает: `{"status": "ok", "event_id": 123}`

#### 4. Web Интерфейс (Jinja2 + HTMX)

**Страница 1: Главная (/)** 
- Список всех кампаний с общей статистикой
- Количество кликов и конверсий для каждой кампании
- Авто-обновление каждые 30 секунд (HTMX)

**Страница 2: Создание кампании (/create)**
- Форма с полями: название, offer URL
- HTMX отправка без перезагрузки страницы
- Автоматический редирект на страницу кампании

**Страница 3: Детали кампании (/campaign/{id})**
- Общая статистика:
  - Email клики
  - Landing клики
  - Конверсии
  - Conversion rate (%)
  - Авто-обновление каждые 10 секунд
  
- Статистика по доменам:
  - Разбивка метрик по каждому домену
  - Conversion rate для каждого домена
  - Сортировка по количеству кликов
  
- Путешествие пользователей:
  - Таблица уникальных email с их событиями
  - Фильтрация по домену (dropdown)
  - Поиск по email (live search с задержкой 500ms)
  - Пагинация "Загрузить еще" (50 записей за раз)
  - Показаны чекмарки (✓/✗) для каждого типа события

### Frontend (HTMX)

#### Динамические обновления без JavaScript:
- `hx-get` - загрузка данных с сервера
- `hx-post` - отправка форм
- `hx-trigger="every 30s"` - автообновление таблиц
- `hx-trigger="keyup changed delay:500ms"` - live search
- `hx-swap` - замена/добавление контента
- `hx-target` - целевой элемент для обновления
- `hx-include` - включение дополнительных полей

#### Responsive дизайн:
- Современный минималистичный интерфейс
- Grid layout для статистики
- Адаптивные таблицы
- Hover эффекты

### Производительность

- Асинхронное подключение к БД (asyncpg)
- Connection pooling (2-10 подключений)
- Индексы на часто запрашиваемые поля
- Агрегированные запросы (COUNT, GROUP BY)
- JSONB для гибкого хранения доп. параметров

### Обработка ошибок

- Валидация всех входных данных через Pydantic
- HTTPException для API ошибок (400, 404)
- Проверка существования кампании перед сохранением события
- Early returns для предотвращения вложенных условий
- Graceful shutdown БД через lifespan context manager

### Безопасность

- Параметризованные SQL запросы (защита от SQL injection)
- Валидация типов данных
- Ограничение длины строк
- CORS можно настроить при необходимости

## 📦 Дополнительные файлы

**example_event_sender.py** - Пример скрипта для отправки событий в трекер

**add_test_data.py** - Скрипт для генерации тестовых данных (3 кампании, ~100 событий)

**QUICKSTART.md** - Пошаговая инструкция по запуску

**Dockerfile & docker-compose.yml** - Контейнеризация приложения

## 🚀 Запуск

### Локально:
```bash
# 1. Установка зависимостей
pip install -r requirements.txt

# 2. Создание БД
createdb tracker_db
psql -d tracker_db -f init.sql

# 3. Настройка .env
cat > .env << EOF
POSTGRES_DB=tracker_db
POSTGRES_USER=postgres
POSTGRES_PASSWORD=password
DATABASE_URL=postgresql://postgres:password@localhost:5432/tracker_db
DEBUG=True
BASE_URL=http://localhost:8000
EOF

# 4. Запуск
uvicorn app.main:app --reload
```

### Docker:
```bash
docker-compose up -d
```

Приложение доступно на: **http://localhost:8000**

## 🎯 Соответствие спецификации

✅ Tech Stack: FastAPI, PostgreSQL, Jinja2, HTMX  
✅ API endpoint `/api/event` с поддержкой всех параметров  
✅ Схема БД: campaigns + events с индексами  
✅ Главная страница со списком кампаний  
✅ Форма создания кампании  
✅ Детальная страница с тремя секциями статистики  
✅ HTMX авто-обновления (10-30 секунд)  
✅ Фильтрация и поиск пользователей  
✅ Пагинация "Load more"  
✅ Responsive дизайн  

## 🔧 Возможные улучшения (out of scope MVP)

- Аутентификация пользователей
- Export данных (CSV, Excel)
- Графики и визуализации (Chart.js)
- Webhooks для уведомлений
- Rate limiting для API
- Кэширование статистики (Redis)
- Логирование в файлы/сервис
- Unit/Integration тесты
- CI/CD pipeline
- Мониторинг (Prometheus/Grafana)

## 📝 Примечания

Код соответствует принципам:
- ✅ SOLID (разделение ответственности)
- ✅ DRY (переиспользование кода)
- ✅ KISS (простота решений)
- ✅ Async/await для I/O операций
- ✅ Type hints для всех функций
- ✅ Валидация через Pydantic
- ✅ Функциональный подход (минимум классов)
- ✅ Комментарии объясняют "почему", не "что"
